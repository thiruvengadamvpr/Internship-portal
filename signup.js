// script.js
const signupForm = document.getElementById('signup-form');

signupForm.addEventListener('submit', (e) => {
    e.preventDefault();
    // Validate form fields (e.g., check email format, password strength)
    // Submit form data to your server or API
});
